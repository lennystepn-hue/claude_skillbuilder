{
  "id": "frontend-accessibility",
  "name": "Accessibility Champion",
  "description": "Comprehensive A11y checker with ARIA, keyboard navigation, screen reader support, and WCAG compliance",
  "category": "Frontend",
  "content": "---\nname: Accessibility Champion\ndescription: Comprehensive A11y checker with ARIA, keyboard navigation, screen reader support, and WCAG compliance\ncategory: Frontend\n---\n\n# Accessibility Champion\n\n## Overview\n\nThis skill provides expert guidance on web accessibility (a11y), including ARIA attributes, keyboard navigation, screen reader support, color contrast, focus management, and WCAG 2.1 compliance. It helps you build inclusive web applications that work for everyone.\n\n## Activation\n\nUse this skill when:\n- Building accessible UI components\n- Implementing keyboard navigation\n- Adding ARIA attributes and roles\n- Testing with screen readers\n- Ensuring WCAG compliance\n- Creating accessible forms and modals\n- Fixing accessibility issues\n\n## Instructions\n\n### 1. Semantic HTML Foundation\n\n**Use Proper HTML Elements:**\n```jsx\n// ❌ Bad: Using divs for everything\n<div onClick={handleClick}>Click me</div>\n<div className=\"heading\">Title</div>\n\n// ✅ Good: Using semantic elements\n<button onClick={handleClick}>Click me</button>\n<h1>Title</h1>\n\n// Navigation structure\n<nav aria-label=\"Main navigation\">\n  <ul>\n    <li><a href=\"/\">Home</a></li>\n    <li><a href=\"/about\">About</a></li>\n    <li><a href=\"/contact\">Contact</a></li>\n  </ul>\n</nav>\n\n// Article structure\n<article>\n  <header>\n    <h2>Article Title</h2>\n    <time dateTime=\"2024-01-15\">January 15, 2024</time>\n  </header>\n  <p>Article content...</p>\n  <footer>\n    <p>Author: John Doe</p>\n  </footer>\n</article>\n\n// Landmarks\n<header role=\"banner\">\n  <nav role=\"navigation\">...</nav>\n</header>\n<main role=\"main\">\n  <section aria-labelledby=\"section-heading\">...</section>\n</main>\n<aside role=\"complementary\">...</aside>\n<footer role=\"contentinfo\">...</footer>\n```\n\n### 2. ARIA Attributes\n\n**ARIA Roles:**\n```jsx\n// Button role for custom buttons\n<div\n  role=\"button\"\n  tabIndex={0}\n  onClick={handleClick}\n  onKeyDown={handleKeyDown}\n>\n  Custom Button\n</div>\n\n// Alert role for notifications\n<div role=\"alert\" aria-live=\"assertive\">\n  Error: Please fill in all required fields\n</div>\n\n// Dialog role for modals\n<div\n  role=\"dialog\"\n  aria-modal=\"true\"\n  aria-labelledby=\"dialog-title\"\n  aria-describedby=\"dialog-description\"\n>\n  <h2 id=\"dialog-title\">Confirm Action</h2>\n  <p id=\"dialog-description\">Are you sure you want to proceed?</p>\n  <button>Confirm</button>\n  <button>Cancel</button>\n</div>\n\n// Tab interface\n<div role=\"tablist\" aria-label=\"Product tabs\">\n  <button\n    role=\"tab\"\n    aria-selected={activeTab === 0}\n    aria-controls=\"panel-0\"\n    id=\"tab-0\"\n  >\n    Description\n  </button>\n  <button\n    role=\"tab\"\n    aria-selected={activeTab === 1}\n    aria-controls=\"panel-1\"\n    id=\"tab-1\"\n  >\n    Reviews\n  </button>\n</div>\n<div\n  role=\"tabpanel\"\n  id=\"panel-0\"\n  aria-labelledby=\"tab-0\"\n  hidden={activeTab !== 0}\n>\n  Description content\n</div>\n```\n\n**ARIA States and Properties:**\n```jsx\n// Expanded/collapsed state\n<button\n  aria-expanded={isOpen}\n  aria-controls=\"menu-content\"\n  onClick={() => setIsOpen(!isOpen)}\n>\n  Menu {isOpen ? '▲' : '▼'}\n</button>\n<div id=\"menu-content\" hidden={!isOpen}>\n  Menu items\n</div>\n\n// Loading state\n<button\n  aria-busy={isLoading}\n  disabled={isLoading}\n>\n  {isLoading ? 'Loading...' : 'Submit'}\n</button>\n\n// Required fields\n<input\n  type=\"text\"\n  aria-required=\"true\"\n  aria-invalid={hasError}\n  aria-describedby=\"error-message\"\n/>\n{hasError && (\n  <span id=\"error-message\" role=\"alert\">\n    This field is required\n  </span>\n)}\n\n// Live regions\n<div aria-live=\"polite\" aria-atomic=\"true\">\n  {statusMessage}\n</div>\n\n// Hidden content\n<button aria-label=\"Close dialog\">\n  <span aria-hidden=\"true\">×</span>\n</button>\n```\n\n### 3. Keyboard Navigation\n\n**Focus Management:**\n```jsx\nimport { useRef, useEffect } from 'react';\n\nfunction Modal({ isOpen, onClose, children }) {\n  const modalRef = useRef(null);\n  const previousFocusRef = useRef(null);\n\n  useEffect(() => {\n    if (isOpen) {\n      // Save current focus\n      previousFocusRef.current = document.activeElement;\n      \n      // Focus first focusable element in modal\n      const focusableElements = modalRef.current.querySelectorAll(\n        'button, [href], input, select, textarea, [tabindex]:not([tabindex=\"-1\"])'\n      );\n      focusableElements[0]?.focus();\n      \n      // Trap focus within modal\n      const handleKeyDown = (e) => {\n        if (e.key === 'Tab') {\n          const firstElement = focusableElements[0];\n          const lastElement = focusableElements[focusableElements.length - 1];\n          \n          if (e.shiftKey && document.activeElement === firstElement) {\n            e.preventDefault();\n            lastElement.focus();\n          } else if (!e.shiftKey && document.activeElement === lastElement) {\n            e.preventDefault();\n            firstElement.focus();\n          }\n        }\n        \n        if (e.key === 'Escape') {\n          onClose();\n        }\n      };\n      \n      document.addEventListener('keydown', handleKeyDown);\n      \n      return () => {\n        document.removeEventListener('keydown', handleKeyDown);\n        // Restore focus\n        previousFocusRef.current?.focus();\n      };\n    }\n  }, [isOpen, onClose]);\n\n  if (!isOpen) return null;\n\n  return (\n    <div\n      ref={modalRef}\n      role=\"dialog\"\n      aria-modal=\"true\"\n      className=\"modal\"\n    >\n      {children}\n    </div>\n  );\n}\n\n// Keyboard-accessible custom button\nfunction CustomButton({ onClick, children }) {\n  const handleKeyDown = (e) => {\n    if (e.key === 'Enter' || e.key === ' ') {\n      e.preventDefault();\n      onClick(e);\n    }\n  };\n\n  return (\n    <div\n      role=\"button\"\n      tabIndex={0}\n      onClick={onClick}\n      onKeyDown={handleKeyDown}\n      className=\"custom-button\"\n    >\n      {children}\n    </div>\n  );\n}\n\n// Skip to main content link\nfunction SkipLink() {\n  return (\n    <a\n      href=\"#main-content\"\n      className=\"skip-link\"\n      style={{\n        position: 'absolute',\n        left: '-9999px',\n        ':focus': {\n          left: '0',\n          top: '0',\n          zIndex: 9999,\n        }\n      }}\n    >\n      Skip to main content\n    </a>\n  );\n}\n```\n\n**Arrow Key Navigation:**\n```jsx\nfunction Menu({ items }) {\n  const [focusedIndex, setFocusedIndex] = useState(0);\n  const itemRefs = useRef([]);\n\n  const handleKeyDown = (e) => {\n    switch (e.key) {\n      case 'ArrowDown':\n        e.preventDefault();\n        setFocusedIndex((prev) => \n          prev < items.length - 1 ? prev + 1 : 0\n        );\n        break;\n      case 'ArrowUp':\n        e.preventDefault();\n        setFocusedIndex((prev) => \n          prev > 0 ? prev - 1 : items.length - 1\n        );\n        break;\n      case 'Home':\n        e.preventDefault();\n        setFocusedIndex(0);\n        break;\n      case 'End':\n        e.preventDefault();\n        setFocusedIndex(items.length - 1);\n        break;\n    }\n  };\n\n  useEffect(() => {\n    itemRefs.current[focusedIndex]?.focus();\n  }, [focusedIndex]);\n\n  return (\n    <ul role=\"menu\" onKeyDown={handleKeyDown}>\n      {items.map((item, index) => (\n        <li key={item.id} role=\"none\">\n          <button\n            ref={(el) => (itemRefs.current[index] = el)}\n            role=\"menuitem\"\n            tabIndex={index === focusedIndex ? 0 : -1}\n            onClick={() => item.onClick()}\n          >\n            {item.label}\n          </button>\n        </li>\n      ))}\n    </ul>\n  );\n}\n```\n\n### 4. Screen Reader Support\n\n**Announcements:**\n```jsx\n// Live region announcer\nfunction LiveAnnouncer({ message, priority = 'polite' }) {\n  return (\n    <div\n      role=\"status\"\n      aria-live={priority}\n      aria-atomic=\"true\"\n      className=\"sr-only\"\n    >\n      {message}\n    </div>\n  );\n}\n\n// Screen reader only text\nfunction SearchButton() {\n  return (\n    <button>\n      <SearchIcon aria-hidden=\"true\" />\n      <span className=\"sr-only\">Search</span>\n    </button>\n  );\n}\n\n// CSS for sr-only class\nconst srOnlyStyles = `\n.sr-only {\n  position: absolute;\n  width: 1px;\n  height: 1px;\n  padding: 0;\n  margin: -1px;\n  overflow: hidden;\n  clip: rect(0, 0, 0, 0);\n  white-space: nowrap;\n  border-width: 0;\n}\n`;\n\n// Descriptive labels\n<input\n  type=\"search\"\n  aria-label=\"Search products\"\n  placeholder=\"Enter product name\"\n/>\n\n// Associated labels\n<label htmlFor=\"email\">Email Address</label>\n<input\n  id=\"email\"\n  type=\"email\"\n  aria-describedby=\"email-help\"\n/>\n<small id=\"email-help\">We'll never share your email</small>\n```\n\n**Image Accessibility:**\n```jsx\n// Informative images\n<img\n  src=\"chart.png\"\n  alt=\"Sales chart showing 20% increase in Q4 2024\"\n/>\n\n// Decorative images\n<img src=\"decorative.png\" alt=\"\" role=\"presentation\" />\n\n// Complex images with long description\n<figure>\n  <img\n    src=\"complex-chart.png\"\n    alt=\"Annual revenue breakdown\"\n    aria-describedby=\"chart-description\"\n  />\n  <figcaption id=\"chart-description\">\n    Detailed breakdown: Product A: 45%, Product B: 30%, Product C: 25%\n  </figcaption>\n</figure>\n\n// Icon buttons\n<button aria-label=\"Add to favorites\">\n  <HeartIcon aria-hidden=\"true\" />\n</button>\n```\n\n### 5. Form Accessibility\n\n**Accessible Form Example:**\n```jsx\nfunction AccessibleForm() {\n  const [errors, setErrors] = useState({});\n  const [touched, setTouched] = useState({});\n  const errorSummaryRef = useRef(null);\n\n  const handleSubmit = (e) => {\n    e.preventDefault();\n    \n    // Validate and collect errors\n    const newErrors = validateForm(formData);\n    setErrors(newErrors);\n    \n    if (Object.keys(newErrors).length > 0) {\n      // Focus error summary\n      errorSummaryRef.current?.focus();\n    } else {\n      // Submit form\n    }\n  };\n\n  return (\n    <form onSubmit={handleSubmit} noValidate>\n      {/* Error summary */}\n      {Object.keys(errors).length > 0 && (\n        <div\n          ref={errorSummaryRef}\n          role=\"alert\"\n          aria-labelledby=\"error-heading\"\n          tabIndex={-1}\n          className=\"error-summary\"\n        >\n          <h2 id=\"error-heading\">Please correct the following errors:</h2>\n          <ul>\n            {Object.entries(errors).map(([field, message]) => (\n              <li key={field}>\n                <a href={`#${field}`}>{message}</a>\n              </li>\n            ))}\n          </ul>\n        </div>\n      )}\n\n      {/* Form fields */}\n      <div className=\"form-group\">\n        <label htmlFor=\"name\">\n          Full Name\n          <abbr title=\"required\" aria-label=\"required\">*</abbr>\n        </label>\n        <input\n          id=\"name\"\n          type=\"text\"\n          required\n          aria-required=\"true\"\n          aria-invalid={!!errors.name}\n          aria-describedby={errors.name ? 'name-error' : 'name-hint'}\n          onBlur={() => setTouched({ ...touched, name: true })}\n        />\n        <span id=\"name-hint\" className=\"hint\">\n          Enter your full legal name\n        </span>\n        {errors.name && touched.name && (\n          <span id=\"name-error\" role=\"alert\" className=\"error\">\n            {errors.name}\n          </span>\n        )}\n      </div>\n\n      <div className=\"form-group\">\n        <fieldset>\n          <legend>Select your plan</legend>\n          <div>\n            <input\n              type=\"radio\"\n              id=\"plan-basic\"\n              name=\"plan\"\n              value=\"basic\"\n            />\n            <label htmlFor=\"plan-basic\">Basic - $9/month</label>\n          </div>\n          <div>\n            <input\n              type=\"radio\"\n              id=\"plan-pro\"\n              name=\"plan\"\n              value=\"pro\"\n            />\n            <label htmlFor=\"plan-pro\">Pro - $19/month</label>\n          </div>\n        </fieldset>\n      </div>\n\n      <div className=\"form-group\">\n        <input\n          type=\"checkbox\"\n          id=\"terms\"\n          required\n          aria-required=\"true\"\n          aria-describedby=\"terms-error\"\n        />\n        <label htmlFor=\"terms\">\n          I agree to the <a href=\"/terms\">terms and conditions</a>\n        </label>\n        {errors.terms && (\n          <span id=\"terms-error\" role=\"alert\" className=\"error\">\n            {errors.terms}\n          </span>\n        )}\n      </div>\n\n      <button type=\"submit\">Submit</button>\n    </form>\n  );\n}\n```\n\n### 6. Color and Contrast\n\n**WCAG Contrast Requirements:**\n```jsx\n// ❌ Bad: Insufficient contrast (2.5:1)\n<button style={{ color: '#999', backgroundColor: '#fff' }}>\n  Click me\n</button>\n\n// ✅ Good: Sufficient contrast (4.5:1 for normal text)\n<button style={{ color: '#595959', backgroundColor: '#fff' }}>\n  Click me\n</button>\n\n// ✅ Good: Sufficient contrast for large text (3:1)\n<h1 style={{ color: '#767676', backgroundColor: '#fff' }}>\n  Heading\n</h1>\n\n// Don't rely on color alone\n// ❌ Bad\n<span style={{ color: 'red' }}>Error</span>\n\n// ✅ Good: Use icons and text\n<span style={{ color: 'red' }}>\n  <ErrorIcon aria-hidden=\"true\" />\n  <span>Error: Invalid input</span>\n</span>\n\n// Focus indicators\nconst buttonStyles = `\n  button:focus-visible {\n    outline: 2px solid #0066cc;\n    outline-offset: 2px;\n  }\n`;\n```\n\n### 7. Component Examples\n\n**Accessible Dropdown:**\n```jsx\nfunction Dropdown({ label, options, value, onChange }) {\n  const [isOpen, setIsOpen] = useState(false);\n  const [focusedIndex, setFocusedIndex] = useState(-1);\n  const buttonRef = useRef(null);\n  const listRef = useRef(null);\n\n  const handleKeyDown = (e) => {\n    switch (e.key) {\n      case 'ArrowDown':\n        e.preventDefault();\n        if (!isOpen) {\n          setIsOpen(true);\n        } else {\n          setFocusedIndex((prev) => \n            prev < options.length - 1 ? prev + 1 : 0\n          );\n        }\n        break;\n      case 'ArrowUp':\n        e.preventDefault();\n        if (isOpen) {\n          setFocusedIndex((prev) => \n            prev > 0 ? prev - 1 : options.length - 1\n          );\n        }\n        break;\n      case 'Escape':\n        setIsOpen(false);\n        buttonRef.current?.focus();\n        break;\n      case 'Enter':\n      case ' ':\n        e.preventDefault();\n        if (isOpen && focusedIndex >= 0) {\n          onChange(options[focusedIndex]);\n          setIsOpen(false);\n          buttonRef.current?.focus();\n        } else {\n          setIsOpen(true);\n        }\n        break;\n    }\n  };\n\n  return (\n    <div className=\"dropdown\">\n      <label id=\"dropdown-label\">{label}</label>\n      <button\n        ref={buttonRef}\n        aria-haspopup=\"listbox\"\n        aria-expanded={isOpen}\n        aria-labelledby=\"dropdown-label\"\n        onClick={() => setIsOpen(!isOpen)}\n        onKeyDown={handleKeyDown}\n      >\n        {value || 'Select an option'}\n      </button>\n      {isOpen && (\n        <ul\n          ref={listRef}\n          role=\"listbox\"\n          aria-labelledby=\"dropdown-label\"\n          tabIndex={-1}\n        >\n          {options.map((option, index) => (\n            <li\n              key={option.value}\n              role=\"option\"\n              aria-selected={option.value === value}\n              className={index === focusedIndex ? 'focused' : ''}\n              onClick={() => {\n                onChange(option);\n                setIsOpen(false);\n                buttonRef.current?.focus();\n              }}\n            >\n              {option.label}\n            </li>\n          ))}\n        </ul>\n      )}\n    </div>\n  );\n}\n```\n\n**Accessible Tooltip:**\n```jsx\nfunction Tooltip({ children, content }) {\n  const [isVisible, setIsVisible] = useState(false);\n  const tooltipId = useId();\n\n  return (\n    <span className=\"tooltip-wrapper\">\n      <button\n        aria-describedby={isVisible ? tooltipId : undefined}\n        onMouseEnter={() => setIsVisible(true)}\n        onMouseLeave={() => setIsVisible(false)}\n        onFocus={() => setIsVisible(true)}\n        onBlur={() => setIsVisible(false)}\n      >\n        {children}\n      </button>\n      {isVisible && (\n        <span id={tooltipId} role=\"tooltip\" className=\"tooltip-content\">\n          {content}\n        </span>\n      )}\n    </span>\n  );\n}\n```\n\n## Examples\n\n### Example 1: Accessible Data Table\n\n```jsx\nfunction DataTable({ data, columns }) {\n  const [sortColumn, setSortColumn] = useState(null);\n  const [sortDirection, setSortDirection] = useState('asc');\n\n  const handleSort = (columnKey) => {\n    if (sortColumn === columnKey) {\n      setSortDirection(sortDirection === 'asc' ? 'desc' : 'asc');\n    } else {\n      setSortColumn(columnKey);\n      setSortDirection('asc');\n    }\n  };\n\n  return (\n    <table>\n      <caption className=\"sr-only\">User data table</caption>\n      <thead>\n        <tr>\n          {columns.map((column) => (\n            <th key={column.key} scope=\"col\">\n              {column.sortable ? (\n                <button\n                  onClick={() => handleSort(column.key)}\n                  aria-sort={\n                    sortColumn === column.key\n                      ? sortDirection === 'asc'\n                        ? 'ascending'\n                        : 'descending'\n                      : 'none'\n                  }\n                >\n                  {column.label}\n                  {sortColumn === column.key && (\n                    <span aria-hidden=\"true\">\n                      {sortDirection === 'asc' ? ' ↑' : ' ↓'}\n                    </span>\n                  )}\n                </button>\n              ) : (\n                column.label\n              )}\n            </th>\n          ))}\n        </tr>\n      </thead>\n      <tbody>\n        {data.map((row, rowIndex) => (\n          <tr key={rowIndex}>\n            {columns.map((column, colIndex) => (\n              <td key={column.key}>\n                {colIndex === 0 ? (\n                  <th scope=\"row\">{row[column.key]}</th>\n                ) : (\n                  row[column.key]\n                )}\n              </td>\n            ))}\n          </tr>\n        ))}\n      </tbody>\n    </table>\n  );\n}\n```\n\n### Example 2: Accessible Accordion\n\n```jsx\nfunction Accordion({ items }) {\n  const [openIndex, setOpenIndex] = useState(null);\n\n  return (\n    <div className=\"accordion\">\n      {items.map((item, index) => (\n        <div key={index} className=\"accordion-item\">\n          <h3>\n            <button\n              aria-expanded={openIndex === index}\n              aria-controls={`panel-${index}`}\n              id={`accordion-${index}`}\n              onClick={() => setOpenIndex(openIndex === index ? null : index)}\n            >\n              {item.title}\n              <span aria-hidden=\"true\">\n                {openIndex === index ? '−' : '+'}\n              </span>\n            </button>\n          </h3>\n          <div\n            id={`panel-${index}`}\n            role=\"region\"\n            aria-labelledby={`accordion-${index}`}\n            hidden={openIndex !== index}\n          >\n            <div className=\"accordion-content\">\n              {item.content}\n            </div>\n          </div>\n        </div>\n      ))}\n    </div>\n  );\n}\n```\n\n### Example 3: Accessible Loading State\n\n```jsx\nfunction LoadingButton({ isLoading, children, ...props }) {\n  return (\n    <button\n      {...props}\n      disabled={isLoading}\n      aria-busy={isLoading}\n      aria-live=\"polite\"\n    >\n      {isLoading ? (\n        <>\n          <span className=\"spinner\" aria-hidden=\"true\" />\n          <span>Loading...</span>\n        </>\n      ) : (\n        children\n      )}\n    </button>\n  );\n}\n\nfunction DataLoader({ url }) {\n  const [status, setStatus] = useState('idle');\n  const [data, setData] = useState(null);\n\n  return (\n    <div>\n      {status === 'loading' && (\n        <div role=\"status\" aria-live=\"polite\">\n          <span className=\"spinner\" aria-hidden=\"true\" />\n          <span>Loading data...</span>\n        </div>\n      )}\n      \n      {status === 'error' && (\n        <div role=\"alert\" aria-live=\"assertive\">\n          <strong>Error:</strong> Failed to load data\n        </div>\n      )}\n      \n      {status === 'success' && data && (\n        <div aria-live=\"polite\">\n          Data loaded successfully\n        </div>\n      )}\n    </div>\n  );\n}\n```\n\n## Testing Tools\n\n1. **axe DevTools**: Browser extension for automated accessibility testing\n2. **WAVE**: Web accessibility evaluation tool\n3. **Lighthouse**: Chrome DevTools accessibility audit\n4. **Screen Readers**: NVDA (Windows), JAWS, VoiceOver (macOS/iOS), TalkBack (Android)\n5. **Keyboard Testing**: Navigate with Tab, Enter, Space, Arrow keys\n6. **Color Contrast Checker**: WebAIM Contrast Checker\n7. **pa11y**: Automated testing tool for CI/CD\n\n## Best Practices\n\n1. **Use semantic HTML** as the foundation\n2. **Keyboard accessibility** is mandatory, not optional\n3. **ARIA should enhance**, not replace semantic HTML\n4. **Test with real screen readers** regularly\n5. **Focus management** is critical for SPAs\n6. **Color contrast** must meet WCAG AA (4.5:1 minimum)\n7. **Provide text alternatives** for all non-text content\n8. **Form labels** must be properly associated\n9. **Error messages** should be clear and actionable\n10. **Automate testing** but also do manual testing\n",
  "prompt": "A skill that provides comprehensive web accessibility expertise including ARIA attributes, keyboard navigation, screen reader support, WCAG compliance, and inclusive design patterns for building accessible web applications.",
  "createdAt": "2024-01-15T10:00:00.000Z",
  "published": true
}
