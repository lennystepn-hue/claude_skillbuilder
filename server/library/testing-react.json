{
  "id": "premade-react-test",
  "name": "react-component-tester",
  "description": "Generate React component tests with Testing Library",
  "category": "Testing",
  "content": "---\nname: react-component-tester\ndescription: When the user needs React component tests\ncategory: Testing\n---\n\n# React Component Tester\n\n## Overview\nGenerate tests for React components using React Testing Library.\n\n## Activation\nActivates when user mentions \"test component\", \"react test\", or \"RTL test\".\n\n## Instructions\n\n1. Test user interactions, not implementation\n2. Use accessible queries (getByRole, getByLabelText)\n3. Test:\n   - Rendering\n   - User events\n   - Async behavior\n   - Error states\n\n## Examples\n\n**Input:** \"tests for LoginForm component\"\n\n**Output:**\n```javascript\nimport { render, screen, waitFor } from '@testing-library/react';\nimport userEvent from '@testing-library/user-event';\nimport LoginForm from './LoginForm';\n\ndescribe('LoginForm', () => {\n  const mockOnSubmit = jest.fn();\n\n  beforeEach(() => {\n    mockOnSubmit.mockClear();\n  });\n\n  it('renders email and password fields', () => {\n    render(<LoginForm onSubmit={mockOnSubmit} />);\n\n    expect(screen.getByLabelText(/email/i)).toBeInTheDocument();\n    expect(screen.getByLabelText(/password/i)).toBeInTheDocument();\n    expect(screen.getByRole('button', { name: /sign in/i })).toBeInTheDocument();\n  });\n\n  it('submits form with entered values', async () => {\n    const user = userEvent.setup();\n    render(<LoginForm onSubmit={mockOnSubmit} />);\n\n    await user.type(screen.getByLabelText(/email/i), 'test@example.com');\n    await user.type(screen.getByLabelText(/password/i), 'password123');\n    await user.click(screen.getByRole('button', { name: /sign in/i }));\n\n    expect(mockOnSubmit).toHaveBeenCalledWith({\n      email: 'test@example.com',\n      password: 'password123'\n    });\n  });\n\n  it('shows validation error for invalid email', async () => {\n    const user = userEvent.setup();\n    render(<LoginForm onSubmit={mockOnSubmit} />);\n\n    await user.type(screen.getByLabelText(/email/i), 'invalid');\n    await user.click(screen.getByRole('button', { name: /sign in/i }));\n\n    expect(screen.getByText(/valid email/i)).toBeInTheDocument();\n    expect(mockOnSubmit).not.toHaveBeenCalled();\n  });\n});\n```",
  "prompt": "A skill that generates React component tests",
  "createdAt": "2024-01-15T10:00:00.000Z",
  "published": true
}
